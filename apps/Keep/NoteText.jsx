

export function NoteText({ info}) {
   
    return ( <section>
       <h1>{info.txt}</h1> 
      
    </section>)
        {/* <input placeholder={info.label} onChange={(ev) => {
            // onAns(ev.target.value)
        }} /> */}
    

}